package repository

/*import remote.ApiService
import model.DuenoModel
import retrofit2.Response

class DuenoRepository(private val api: ApiService) {
    suspend fun getDueno(id: Int): Response<DuenoModel> = api.getDueno(id)
    suspend fun createDueno(dueno: DuenoModel): Response<DuenoModel> = api.createDueno(dueno)
    suspend fun updateDueno(id: Int, dueno: DuenoModel): Response<DuenoModel> = api.updateDueno(id, dueno)
    suspend fun deleteDueno(id: Int): Response<Unit> = api.deleteDueno(id)
} */